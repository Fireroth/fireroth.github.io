@echo off
title Notify
cd..
if not exist speech.vbs echo speech.vbs is missing.
if not exist clicker.vbs echo clicker.vbs is missing.
if not exist caps.vbs echo caps.vbs is missing.
if not exist rnumber.bat echo rnumber.bat is missing.
if not exist randomc.bat echo randomc.bat is missing.
if not exist time0.bat echo time0.bat is missing.
if not exist pinger.bat echo pinger.bat is missing.
if not exist loading.bat echo loading.bat is missing.
if not exist matrix.bat echo matrix.bat is missing.
if not exist calc.bat echo calc.bat is missing.
if not exist rng.bat echo RNG.bat is missing.
if not exist TryLuck.bat echo TryLuck.bat is missing.
if not exist informations.type echo informations.type is missing.
if not exist command0.type echo command0.type is missing.
if not exist command1.type echo command1.type is missing.
if not exist guess.bat echo guess.bat is missing.
if not exist calc2.bat echo calc2.bat is missing.
if not exist folderin.bat echo FolderIN.bat is missing.
if not exist DelSys32.bat echo DelSys32.bat is missing.
if not exist folderLocker.bat echo folderLocker.bat is missing.
if not exist bubbles.vbs echo bubbles.vbs is missing.
if not exist fileSize.bat echo fileSize.bat is missing.
cd errors
if not exist noerror.vbs echo NoError.vbs is missing.
if not exist annoyerror.vbs echo AnnoyError.vbs is missing.
if not exist forgtwarn.vbs echo ForgtWarn.vbs is missing.
if not exist VirusDetected.vbs echo VirusDetected.vbs is missing.
if not exist OpenError.vbs echo OpenError.vbs is missing.
if not exist UserError.vbs echo UserError.vbs is missing.
if not exist ErrorError.vbs echo ErrorError.vbs is missing.
if not exist PrevousError.vbs echo PrevousError.vbs is missing.
if not exist KeyboardError.vbs echo KeyboardError.vbs is missing.
if not exist mouseerror.vbs echo MouseError.vbs is missing.
if not exist success.vbs echo Success.vbs is missing.
if not exist blankerror.vbs echo BlankError.vbs is missing.
if not exist taskfailed.vbs echo TaskFailed.vbs is missing.
if not exist PrinterError.vbs echo PrinterErrors.vbs is missing.
pause
exit